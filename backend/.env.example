SUPABASE_URL=https://sftbahfyyllzvtehdlwe.supabase.co
# Supabase > Project Settings > API > service_role key (SECRET - server only, never put in the frontend or GitHub)
SUPABASE_SERVICE_KEY=
# Long random string, e.g. run: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=
# Frontend ORIGIN only (no path, no trailing slash)
FRONTEND_URL=https://nexus-wits-cps.github.io
